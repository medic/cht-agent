# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Choose between API mode (requires API key) or CLI mode (uses Claude Code CLI)
#
# LLM_PROVIDER options:
#   - anthropic   : Use Anthropic API (requires ANTHROPIC_API_KEY)
#   - claude-cli  : Use Claude Code CLI (requires 'claude' CLI installed)
#
# CLI mode is ideal for:
#   - Claude MAX subscribers (no API key needed)
#   - Local development without API costs
#   - Teams without shared API keys
LLM_PROVIDER=anthropic

# Anthropic API Key (required only if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_api_key_here

# LLM Model to use (optional)
# For API mode: claude-opus-4-5-20251101, claude-sonnet-4-20250514
# For CLI mode: uses your CLI's default model
LLM_MODEL=claude-opus-4-5-20251101

# ============================================================================
# Claude Code CLI Configuration (only used if LLM_PROVIDER=claude-cli)
# ============================================================================
# Path to Claude CLI executable (default: claude)
# CLAUDE_CLI_PATH=claude

# CLI timeout in milliseconds (default: 300000 = 5 minutes)
# CLAUDE_CLI_TIMEOUT=300000

# Max agentic turns for CLI (default: 1 for simple completions)
# CLAUDE_CLI_MAX_TURNS=1

# ============================================================================
# MCP Server Configuration
# ============================================================================
# MCP Server URL for CHT documentation search (Kapa.AI)
# Local development: http://localhost:3000/mcp (unlimited requests)
# Contributors/dev: https://mcp-docs.dev.medicmobile.org/mcp (10 req/user limit)
# Production: <your-production-mcp-url>
MCP_SERVER_URL=https://mcp-docs.dev.medicmobile.org/mcp

# ============================================================================
# CHT Project Configuration
# ============================================================================
# Path to cht-core codebase (required for development phase)
# This allows agents to read existing code patterns and write generated code
CHT_CORE_PATH=/path/to/cht-core
