# A draft with no frontmatter

This file has no YAML frontmatter block and is not on the prose allowlist, so it
must be reported as `missing-frontmatter` rather than silently skipped. The
default `validate-schema` behaviour is to skip it, which is how an unkeyed file
can enter the corpus unnoticed.
